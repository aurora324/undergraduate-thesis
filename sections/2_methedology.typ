= 研究方法

== 问题定义

在单源域泛化（SDG）设定下，训练阶段仅提供一个带标注的源域数据集 $cal(D)_s = {(x_i, y_i)}_(i=1)^N$，其中 $x_i$ 为输入眼底图像，$y_i in {0, 1}^{H times W}$ 为对应的像素级血管标签。目标域数据集记为 $cal(D)_t$，其标注在训练过程中不可用。该任务要求模型 $f_theta$ 只基于源域训练，却能在分布不同的未见目标域上保持稳定分割性能。设源域和目标域的数据分布分别为 $P_s (x, y)$ 与 $P_t (x, y)$，当二者的边缘分布满足 $P_s (x) != P_t (x)$ 时，模型会受到域偏移影响。本文的优化目标可表示为：

$ min_theta EE_((x,y) tilde P_t) [cal(L)(f_theta (x), y)] $

其中 $cal(L)$ 表示分割损失函数，可采用交叉熵损失或 Dice 损失@hinton2012improving。对于前景血管像素占比较低的情况，Focal Loss@lin2017focal 可通过降低易分类样本权重来缓解类别不平衡。

== 方法概述

为缓解未见域上的性能下降，本文提出 BIRF-SDG（Band Importance Aware Random Frequency Filter based Single-source Domain Generalization）框架。该方法认为，不同频率带对血管结构保持和分割判别的贡献并不相同：部分频带承载边界和细小血管等结构信息，应在增强过程中被保护；另一部分频带更多对应风格或非关键变化，可以用于构造跨域扰动。

如@fig-main-framework 所示，BIRF-SDG 由三个部分组成：首先将输入图像映射到频域并划分频带；随后分别估计各频带对结构保持和分割输出的影响；最后根据评分结果在可扰动频带上执行加权随机滤波，生成用于训练的增强样本。

#figure(
  image("../images/main.png", width: 95%),
  caption: [BIRF-SDG 框架概览。BIRF 采用加权机制评估不同频率带对分割性能的贡献，并通过随机频带滤波策略生成增强图像。多流分割架构整合这些增强输入，通过注意力机制利用频率感知特征，最终形成一个高效的单源域泛化框架。],
) <fig-main-framework>

== 频域增强

为了使模型不过度依赖源域中的固定频谱模式，本文在训练阶段引入频域扰动。与直接改变整幅图像风格不同，我们关注单个频率带被抑制后对分割结果和结构信息的影响，并据此判断哪些频带适合被增强。

=== 傅里叶变换

本文利用快速傅里叶变换（Fast Fourier Transform, $cal(F)$）将图像 $x$ 从空间域映射到频域：

$ X = cal(F)(x) $

其中 $X$ 表示复数频谱，可进一步拆分为振幅谱 $A = |X|$ 和相位谱 $P = angle X$。振幅谱刻画不同频率成分的能量分布，相位谱则更多保留图像空间结构。频域操作完成后，图像可通过逆快速傅里叶变换（inverse Fast Fourier Transform, $cal(F)^(-1)$）映射回空间域：

$ x = cal(F)^(-1)(X) $

=== 频带划分与掩码

为衡量不同频带的重要性，本文采用选择性抑制的方式观察模型响应。具体地，我们定义频域掩码 $m$，并通过逐元素乘法控制频谱中指定区域是否被保留：

$ X_("filtered") = X dot m $

其中 $dot$ 表示逐元素乘法。为便于描述，掩码被写成关于半径 $r$ 的一维函数，$r$ 表示频率位置到频谱中心的欧氏距离。当函数值为 0 时，对应频带被抑制；函数值为 1 时，该频带保持不变。本文将频谱划分为 $n$ 个等宽频带，并依次分析每个频带被抑制后的影响：

$ m_i (r) = 1_(frac(i,n) <= r < frac(i+1,n)) $

其中 $i$ 是 0 到 $n - 1$ 之间的整数，定义了丢弃掩码的边界，$1_(c)$ 是指示函数。

基于上述掩码操作，可以进一步区分与结构保持相关的频带集合 $cal(B)_s$ 和与分割判别相关的频带集合 $cal(B)_v$。设 $P_s(.)$ 与 $P_v(.)$ 分别表示结构保留判定和分割保留判定，则有：

$ cal(B)_s = {b_i | P_s(m_i dot x) = 1} $

$ cal(B)_v = {b_i | P_v(m_i dot x) = 1} $

通过这种频带级分析，增强过程不再随机作用于整个频谱，而是优先识别并保护分割任务依赖的频率区域。该思想与逆问题求解中的正则化技术具有一定相似性，即在引入扰动时限制解空间中的不稳定变化@dobado1997inverse。

== 频带重要性感知随机滤波

眼底血管通常具有细长、分叉多和边界弱的特点，因此结构信息对分割结果尤为关键。如果随机滤波直接破坏包含边缘和分支的频带，模型可能获得更多风格变化，却失去必要的血管形态约束。为避免这一问题，本文引入高频模块评估频带对结构信息的贡献。

=== 结构评分与分割评分

高频模块记为 $cal(H)$，通过以下公式获取图像的显著结构：

$ H = cal(H)(x) = x - g(x; sigma) $

其中 $g(x; sigma)$ 表示标准差为 $sigma$ 的高斯平滑滤波器。原图与平滑结果相减后得到的 $H$ 会突出边缘、细小纹理和局部结构，这些信息对血管边界定位较为关键。

若抑制某一频带后高频结构发生明显变化，则说明该频带对结构保持较为重要。本文用结构评分 $s_s$ 描述这种影响@li2022domain，其计算方式为比较原始图像与滤波图像的高频成分差异：

$ s_s (b_i) = sum|cal(H)(x) - cal(H)(m_i dot x)| $

其中 $x$ 是原始图像，$b_i$ 表示第 $i$ 个频带，$m_i$ 是对应的掩码。评分越高，表示该频带对结构保留越重要。

除了结构差异外，本文还直接考察频带抑制对分割输出的影响。若移除某一频带后预测结果变化较小，则该频带更适合作为增强对象。对应的分割评分记为 $s_v$：

$ s_v (b_i) = sum|f(x) - f(m_i dot x)| $

其中 $f$ 表示分割网络的前向传播过程。评分越低，表示该频带对分割越不重要，可以进行增强。

综合上述两类信息，最终评分 $s$ 定义为：

$ s(b_i) = alpha dot s_s (b_i) + s_v (b_i) $

其中 $alpha$ 控制结构评分在总评分中的权重，默认取 1。该设计使结构保持和分割稳定性共同影响频带排序。根据评分结果，可得到结构关键频带 $cal(B)_s = {b_i | s_s (b_i) > "mean"(s_s)}$ 以及分割关键频带 $cal(B)_v = {b_i | s_v (b_i) < "mean"(s_v)}$。

=== 随机频带滤波

得到频带评分后，滤波过程仍需避免误删关键结构信息。因此，本文将可扰动范围限制在结构评分较低的频带中，并在该集合内进行随机采样。可丢弃频带集合 $D$ 定义为：

$ D = {b_i | s_s (b_i) < "mean"(s_s)} $

随后在集合 $D$ 中生成随机掩码，并用重建后的增强图像参与训练：

$ x_("aug") = cal(F)^(-1)(X dot m_D) $

其中 $m_D$ 表示由可丢弃频带生成的随机掩码。其采样概率由分割评分决定：

$ p(b_i) = "softmax"(-s_v (b_i)) $

因此，对分割输出影响较小的频带更容易被滤波，而对结果敏感的频带会以更高概率被保留。

#figure(
  image("../images/choose.png", width: 95%),
  caption: [随机频带滤波模块示意图。模型根据结构评分和分割评分区分关键频带与非关键频带，并在可丢弃频带集合中随机生成滤波掩码，最终重建增强图像。],
) <fig-random-filter>


== 分割网络整体结构

为进一步利用频域增强带来的结构信息，本文构建了多任务分割网络。该网络在常规分割分支之外加入高频重建分支，使模型在学习血管掩膜的同时关注边界和细节恢复，从而增强对频域结构变化的感知能力。

如@fig-main-framework 所示，网络包含编码器、高频解码器和分割解码器三个主要部分。编码器以 ResNet@he2016deep 或 DenseNet@huang2017densely 为骨干提取多尺度特征；高频解码器借助跳跃连接接收编码器特征，并重建图像中的高频结构以强化边界信息；分割解码器负责输出血管掩膜，并通过注意力机制与高频分支进行特征交互。

两个解码器之间通过注意力机制进行信息交互，使分割分支能够利用高频分支提供的边界线索。本文采用空间和通道注意力机制（CBAM）@woo2018cbam 强化特征选择，并借助注意力建模通道和空间位置之间的关系@vaswani2017attention@wang2018non。

#figure(
  image("../images/HFC.png", width: 95%),
  caption: [多流分割网络结构示意图。编码器提取多尺度特征后，分别由高频解码器和分割解码器恢复边界信息与血管掩膜，并通过注意力机制进行信息交互。],
) <fig-multistream-network>

@fig-multistream-network 展示了高频解码器、分割解码器和损失函数之间的整体关系。

设 $y$ 为真实血管标签，$hat(y)$ 为输入图像 $x$ 的预测分割结果，分割分支采用交叉熵损失进行优化：

$ cal(L)_("seg") = - sum(y log(hat(y)) + (1 - y) log(1 - hat(y))) $


高频分支则通过约束解码结果与高频成分之间的差异来学习边界特征：

$ cal(L)_("bdr") = sum|cal(H)(x) - "HFDecoder"(x)| $

最终训练目标由分割损失和边界损失共同构成：

$ cal(L) = cal(L)_("seg") + beta dot cal(L)_("bdr") $

其中 $beta$ 用于调节边界损失在总目标中的权重，默认取 1。

训练过程分为预热和增强两个阶段。前 20 个 epoch 仅使用原始图像训练分割网络，使模型先形成稳定的基础分割能力；随后 30 个 epoch 加入频带评分和随机滤波增强。学习率初始设为 $1 times 10^(-4)$，并采用余弦退火策略逐步衰减至零。当验证集性能连续 10 个 epoch 没有提升时，训练提前终止。

== 算法流程

BIRF-SDG 的训练流程如算法 1 所示。整体过程先建立稳定分割模型，再根据频带评分生成增强样本，从而在扩展训练分布的同时保护关键血管结构。@fig-training-procedure 概括了预热阶段和增强阶段的关系。

#figure(
  image("../images/train.png", width: 95%),
  caption: [BIRF-SDG训练流程示意图。训练过程分为预热阶段和增强阶段：前20个epoch仅使用原始图像训练，后30个epoch引入频带评分、随机滤波和多流网络联合优化。],
) <fig-training-procedure>

首先初始化网络参数 $theta$ 和频带评分模块。对于每个训练批次 $(x, y)$，将图像转换到频域 $X = cal(F)(x)$，计算各频带的结构评分 $s_s$ 与分割评分 $s_v$，并确定可丢弃频带集合 $D = {b_i | s_s (b_i) < "mean"(s_s)}$。随后根据 $p(b_i) = "softmax"(-s_v (b_i))$ 采样滤波掩码 $m_D$，生成增强图像 $x_("aug") = cal(F)^(-1)(X dot m_D)$。模型前向传播得到分割结果 $hat(y)$ 和高频重建结果 $H$，再根据总损失 $cal(L) = cal(L)_("seg") + beta dot cal(L)_("bdr")$ 反向更新参数 $theta$。训练完成后返回模型 $f_theta$。

该流程的核心在于三点：其一，频带评分使模型能够区分结构关键频带和可扰动频带；其二，加权随机滤波限制增强作用范围，降低破坏血管结构的风险；其三，多任务学习同时约束分割结果和边界重建，提高模型对细小血管结构的敏感性。
