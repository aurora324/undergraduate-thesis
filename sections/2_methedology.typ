= 研究方法

== 问题定义

在单源域泛化（SDG）中，我们有一个源域数据集 $cal(D)_s = {(x_i, y_i)}_(i=1)^N$，包含 $N$ 个带有标签的输入样本，其中 $y_i in {0, 1}^{H times W}$ 是输入数据 $x_i$ 对应的分割标签。目标域数据集记为 $cal(D)_t$，缺乏分割标签。一般而言，SDG 的目的是使在源域 $cal(D)_s$ 上训练的模型 $f_theta$ 能够在未见目标域 $cal(D)_t$ 上同样良好地工作。域偏移问题的数学描述如下：假设源域和目标域的数据分布分别为 $P_s (x, y)$ 和 $P_t (x, y)$，当 $P_s (x) eq P_t (x)$ 时，即存在域偏移。我们的目标是学习一个模型，使其在源域上训练后，在目标域上也能保持较好的性能：

$ min_theta EE_((x,y) tilde P_t) [cal(L)(f_theta (x), y)] $

其中 $cal(L)$ 是损失函数，通常采用交叉熵损失或 Dice 损失 @hinton2012improving。对于类别不平衡的分割任务，Focal Loss @lin2017focal 通过降低易分类样本的权重，使模型更关注难分类样本，从而提高分割精度。

== 方法概述

针对未见域性能下降的挑战，我们提出了一种用于视网膜血管分割的单源域泛化（SDG）算法，称为 BIRF-SDG（Band Importance Aware Random Frequency Filter based Single-source Domain Generalization）。该方法的核心思想是：图像的频率成分可分为与结构相关的频带和与分割相关的频带，通过自适应地识别和分离这些频带，我们可以在增强数据多样性的同时保留关键的结构信息。

如 @fig-main-framework 所示，BIRF-SDG 框架包含三个主要组件：频域分析模块将图像转换到频域并划分为多个频率带；频带评分模块评估每个频率带对结构保留和分割任务的重要性；随机滤波模块基于评分结果对非关键频率带进行加权随机滤波。以下章节将详细介绍这些组件。

#figure(
  image("../images/main.png", width: 95%),
  caption: [BIRF-SDG 框架概览。BIRF 采用加权机制评估不同频率带对分割性能的贡献，并通过随机频带滤波策略生成增强图像。多流分割架构整合这些增强输入，通过注意力机制利用频率感知特征，最终形成一个高效的单源域泛化框架。],
) <fig-main-framework>

== 频域增强

增强模型对目标域的泛化能力需要降低其对特定频域特征的依赖，并增强其在某些频率带缺失时重建分割结构的能力。为此，我们引入频域增强，并系统研究不同频率带如何影响分割性能和结构完整性。

=== 傅里叶变换

快速傅里叶变换（Fast Fourier Transform, $cal(F)$）用于提取图像 $x$ 的频域信息：

$ X = cal(F)(x) $

其中 $X$ 是复数频谱，可以分解为振幅谱 $A = |X|$ 和相位谱 $P = angle X$。振幅谱反映了图像中各个频率成分的强度，而相位谱则包含了图像的结构信息。相应地，频域信息也可以通过逆快速傅里叶变换（inverse Fast Fourier Transform, $cal(F)^(-1)$）转换回原始图像：

$ x = cal(F)^(-1)(X) $

=== 频带划分与掩码

具体而言，我们通过选择性丢弃某个频带内的信息来评估每个频率带的贡献。为在频域实现这种抑制，我们引入映射函数 $m$，通过逐元素乘法来调制频率成分：

$ X_("filtered") = X dot m $

其中 $dot$ 表示逐元素乘法。为简化起见，滤波过程使用一维函数 $m(r)$ 建模，其中 $r$ 表示距频率谱中心的欧氏距离，函数输出 0 表示抑制给定频率带，输出 1 表示保留原始频率内容。与传统滤波器类似，我们将图像划分为 $n$ 个频率带，一种解耦方法是每次抑制等长的频率带以确定每个频率带的影响：

$ m_i (r) = 1_{{ frac(i,n) <= r < frac(i+1,n) }} $

其中 $i$ 是 0 到 $n - 1$ 之间的整数，定义了丢弃掩码的边界，$1_{{c}}$ 是指示函数。

通过这种方法，我们可以分别找到对应结构信息的频率带和对应分割任务的频率带，分别记为 $cal(B)_s$ 和 $cal(B)_v$，并将它们分离。这一设计旨在防止后续任务中丢失分割所需的频率带，公式如下：

$ cal(B)_s = {b_i | m_i dot x "保留结构信息"} $

$ cal(B)_v = {b_i | m_i dot x "保留分割信息"} $

总之，我们提出的增强机制通过在频域中使用等长掩码来区分不同频率带，防止模型在分割任务中遗漏依赖的频率带，同时这增强了数据的多样性，使我们的分割方法能够更好地适应各种未见过的视网膜血管数据集。这种频域分析方法与逆问题求解中的正则化技术有相似之处 @dobado1997inverse。

== 频带重要性感知随机滤波

在眼底图像中，结构信息起着非常重要的作用，血管的边缘和分支结构对于准确分割至关重要。传统的随机频率滤波方法可能会破坏这些结构，导致分割性能下降。因此，我们使用高频模块来评估频率带结构信息的重要性。

=== 结构评分与分割评分

高频模块记为 $cal(H)$，通过以下公式获取图像的显著结构：

$ H = cal(H)(x) = x - g(x; sigma) $

其中 $g(x; sigma)$ 表示标准差为 $sigma$ 的高斯滤波器。高频成分 $H$ 突出了图像中的边缘和细节信息，对于血管分割尤为重要。

如果移除某个频率带导致结构信息发生显著变化，则该频率带具有更高的结构重要性，记为 $s_s$ @li2022domain。结构评分通过比较原始图像和滤波后图像的高频成分差异来计算：

$ s_s (b_i) = sum|cal(H)(x) - cal(H)(m_i dot x)| $

其中 $x$ 是原始图像，$b_i$ 表示第 $i$ 个频带，$m_i$ 是对应的掩码。评分越高，表示该频带对结构保留越重要。

频率带的权重还通过分割任务来评估，如果移除某个频率带对分割几乎没有影响，则该频率带具有更高的分割评分，记为 $s_v$，分割评分公式为：

$ s_v (b_i) = sum|f(x) - f(m_i dot x)| $

其中 $f$ 表示分割网络的前向传播过程。评分越低，表示该频带对分割越不重要，可以进行增强。

因此，最终评分记为 $s$，综合考虑结构评分和分割评分：

$ s(b_i) = alpha dot s_s (b_i) + (1 - alpha) dot s_v (b_i) $

其中 $alpha$ 表示平衡 $s_s$ 和 $s_v$ 的超参数，默认值为 1。通过调整 $alpha$，我们可以控制结构保留和分割性能之间的权衡。基于评分结果，我们可以识别出结构关键频带 $cal(B)_s = {b_i | s_s (b_i) > "mean"(s_s)}$ 和分割关键频带 $cal(B)_v = {b_i | s_v (b_i) < "mean"(s_v)}$。

=== 随机频带滤波

尽管我们已经获得了每个频率带的权重，但我们不能保证所有频率带都被正确分类。我们希望确保滤波器尽可能正确地工作，因此我们仅丢弃对结构信息没有影响的频率带，并根据权重进行随机滤波。为了仅丢弃对结构信息没有影响的频率带，最终可以丢弃的频率带 $D$ 定义为：

$ D = {b_i | s_s (b_i) < "mean"(s_s)} $

我们将在 $D$ 中进行随机滤波作为模型的最终输入图像，以提高模型的泛化能力。随机滤波过程公式为：

$ x_("aug") = cal(F)^(-1)(X dot m_D) $

其中 $m_D$ 是基于 $D$ 中频带权重生成的随机掩码。掩码的生成概率与频带的分割评分相关：

$ p(b_i) = "softmax"(-s_v (b_i)) $

这样，对分割影响较小的频带有更高的概率被滤波，而对分割重要的频带则会被保留。

== 分割网络整体结构

我们设计了一个融合上述机制的多任务分割模型。在通用深度学习框架的基础上，我们提出了一个多流网络，有效地将滤波任务与分割任务相结合。这一设计使模型能够同时处理这两个任务，从而增强其从频域特征学习的能力，同时提高分割性能。

如 @fig-main-framework 所示，我们的网络由三个主要模块组成：编码器采用 ResNet @he2016deep 或 DenseNet @huang2017densely 作为骨干网络提取多尺度特征；高频解码器通过跳跃连接与编码器相连，重建图像的高频成分，增强边界信息；分割解码器对增强后的图像进行分割，通过注意力机制与高频解码器交互。

两个解码器通过注意力机制相连，使分割解码器能够利用高频解码器提取的边界信息。我们在解码器中引入了空间和通道注意力机制（CBAM）@woo2018cbam，增强了模型的特征提取能力。注意力机制通过显式建模通道间和空间上的关系，提升了模型对重要特征的感知能力 @vaswani2017attention @wang2018non。

相应地，假设 $y$ 是真实标签，$hat(y)$ 是输入图像 $x$ 的分割结果。我们使用交叉熵函数作为分割的目标函数：

$ cal(L)_("seg") = - sum(y log(hat(y)) + (1 - y) log(1 - hat(y))) $

高频解码器的结果与高频成分的结果获得边界特征损失，公式为：

$ cal(L)_("bdr") = sum|cal(H)(x) - "HFDecoder"(x)| $

最终损失函数为分割损失和边界损失的加权和：

$ cal(L) = cal(L)_("seg") + beta dot cal(L)_("bdr") $

其中 $beta$ 表示平衡 $cal(L)_("seg")$ 和 $cal(L)_("bdr")$ 的超参数，默认值为 1。

在训练过程中，我们采用以下策略：预热阶段前 20 个 epoch 仅使用原始图像进行训练，使模型学习基本的分割能力；增强阶段后续 30 个 epoch 引入频域增强，逐渐提高增强强度；学习率调度使用余弦退火策略，初始学习率为 $1 times 10^(-4)$，逐渐衰减至零；早停机制如果在验证集上性能连续 10 个 epoch 没有提升，则停止训练。

== 算法流程

BIRF-SDG 的完整训练流程如算法 1 所示。该算法通过自适应的频域增强，在增加数据多样性的同时保留了关键的结构信息，从而提高了模型的泛化能力。

算法 1：BIRF-SDG 训练算法

输入：源域数据集 $cal(D)_s$，频带数量 $n$，超参数 $alpha, beta$

输出：训练好的模型 $f_theta$

首先初始化网络参数 $theta$ 和评分模块。然后对于每个训练 epoch，对于每个训练批次 $(x, y)$，将图像 $x$ 转换到频域 $X = cal(F)(x)$，计算每个频带的结构评分 $s_s$ 和分割评分 $s_v$，确定可丢弃频带集合 $D = {b_i | s_s (b_i) < "mean"(s_s)}$，基于权重随机生成滤波掩码 $m_D$，其中 $p(b_i) = "softmax"(-s_v (b_i))$，生成增强图像 $x_("aug") = cal(F)^(-1)(X dot m_D)$，前向传播计算分割结果 $hat(y)$ 和高频重建 $H$，计算总损失 $cal(L) = cal(L)_("seg") + beta dot cal(L)_("bdr")$，反向传播更新参数 $theta$。最后返回训练好的模型 $f_theta$。

该算法的关键创新在于频带评分机制，通过评估不同频率带对分割性能和结构信息的影响，自适应地识别关键频带；加权随机滤波，基于评分结果，仅对不影响结构信息的频带进行增强，避免了过度增强导致的性能下降；多任务学习，通过同时优化分割损失和边界损失，增强了模型对血管结构的感知能力。
