= 实验

== 实验设置

=== 数据集

本章通过跨数据集眼底血管分割实验评估 BIRF-SDG 的有效性。实验重点考察模型从单一源域训练后迁移到未见目标域时的表现，并通过对比实验、消融实验和定性分析验证各模块的作用。

@tbl-dataset 汇总了本文使用的数据集。DRIVE 被设为主要标注源域，共包含 40 张眼底图像，其中训练集和测试集各 20 张，原始分辨率为 584 乘以 565 像素。EyePACS 提供 35106 张未标注眼底图像，本文仅将其作为未标注补充数据及多源域基线方法的参考，不将其用于 BIRF-SDG 的监督训练。相关眼底数据集已广泛用于糖尿病视网膜病变研究@ting2017development。

目标域选取 IOSTAR、LES-AV 和 CHASE-DB1。IOSTAR 由扫描激光眼底镜（SLO）采集，与普通眼底相机在成像方式上存在差别；LES-AV 图像分辨率较高，能够呈现更密集的血管分支和局部细节；CHASE-DB1 主要由儿童眼底图像组成，与成人眼底数据在人群特征上不同。三个目标域覆盖设备、分辨率和受试者群体等多类变化，可用于检验模型跨域泛化能力。

实验中，所有输入图像均裁剪为 512 乘以 512 像素。为保证比较条件一致，各方法采用相同的数据划分、预处理流程和目标域测试方式。训练阶段采用 Adam 优化器@kingma2015adam，beta1 与 beta2 分别设为 0.9 和 0.999，初始学习率为 1 乘以 10 的负 4 次方，并使用余弦退火策略衰减至零。模型训练 50 个 epoch，其中前 20 个 epoch 作为预热阶段，后 30 个 epoch 引入频域增强，批次大小为 4。医学图像任务中样本规模与模型性能之间的关系仍值得进一步研究@liu2022do；AdamW@loshchilov2019decoupled 可通过解耦权重衰减提升训练稳定性。本文将频谱划分为 10 个频带，并将频带评分权重 alpha 和边界损失权重 beta 均设为 1.0，使结构评分与分割评分共同参与频带重要性估计。


#figure(
  table(
    columns: (auto, auto, auto, auto, auto, auto),
    inset: 8pt,
    align: center,
    stroke: (x, y) => if y == 0 or y == 1 or y == 6 {
      (top: 0.5pt + black, bottom: 0.5pt + black)
    } else {
      (bottom: 0.5pt + black)
    },
    table.header(
      [数据集], [图像数量], [分辨率], [成像设备], [特点], [用途],
    ),
    [DRIVE], [40 (20训练/20测试)], [584×565], [眼底相机], [标准基准数据集，血管标注精细], [源域],
    [EyePACS], [35106], [多种], [多种眼底相机], [大规模未标注数据], [未标注补充],
    [IOSTAR], [30], [1024×1024], [SLO扫描激光眼底镜], [与眼底相机图像特性差异大], [目标域],
    [LES-AV], [22], [1620×1080], [高分辨率眼底相机], [血管结构复杂，高分辨率], [目标域],
    [CHASE-DB1], [28], [999×960], [儿童眼底相机], [儿童眼底图像，与成人差异大], [目标域],
  ),
  caption: [实验数据集信息对比],
) <tbl-dataset>

本文采用 DICE、MCC 和 IoU 评价分割性能。DICE 用于衡量预测掩膜与人工标注之间的重叠情况；MCC 将真阳性、真阴性、假阳性和假阴性同时纳入计算，适合评价类别不平衡条件下的二分类结果；IoU 则表示预测区域与标注区域交集在二者并集中的占比。设 $"TP"$、$"TN"$、$"FP"$ 和 $"FN"$ 分别为真阳性、真阴性、假阳性和假阴性，三种指标定义如下：

$ "DICE" = (2 dot "TP") / (2 dot "TP" + "FP" + "FN") $

$ "MCC" = ("TP" dot "TN" - "FP" dot "FN") / sqrt(("TP" + "FP")("TP" + "FN")("TN" + "FP")("TN" + "FN")) $

$ "IoU" = "TP" / ("TP" + "FP" + "FN") $

=== 实现细节

#figure(
  grid(
    columns: 1fr,
    row-gutter: 6pt,
    [
      #image("../images/Drive.png", width: 92%)
      #align(center)[(a) DRIVE标注样本]
    ],
    [
      #image("../images/eyepacs.png", width: 92%)
      #align(center)[(b) EyePACS未标注样本]
    ],
  ),
  caption: [源域与未标注补充数据示例。(a) DRIVE数据集样本，包含彩色眼底图像及两位专家标注，分辨率为584×565像素，使用Canon CR5非散瞳相机拍摄。(b) EyePACS数据集样本，包含来自不同来源的眼底图像，分辨率和成像设备多样，通常用作多源域泛化的补充数据。两个数据集在图像质量、亮度和对比度上存在显著差异，体现了真实场景中的域偏移问题。],
) <fig-source-datasets>

=== 评估指标


上述指标的取值范围均为 0 到 1，数值越大说明预测结果与真实标签越一致。

== 对比实验

=== 对比方法

对比方法涵盖频域增强、多模态分割和 SDG 等方向。FACT 通过振幅谱替换生成增强样本，FedDG 利用对比学习促进多源域频率信息共享。CSCA 将注意力机制引入多模态分割框架。GIN-IPA 采用因果启发的数据增强策略，FreeSDG 结合频域混合与边界感知学习，FD-SDG 通过频率丢弃提高模型泛化能力。与这些方法比较，可以更充分地评估 BIRF-SDG 在跨域血管分割中的表现。

=== 定量结果

@tbl-comparison 给出了不同方法在三个目标域上的定量结果，评价指标包括 DICE、MCC 和 IoU。

#figure(
  table(
    columns: (auto, auto, auto, auto, auto, auto, auto, auto, auto, auto),
    inset: 4pt,
    align: center,
    stroke: (x, y) => if y == 0 or y == 1 or y == 8 {
      (top: 0.5pt + black, bottom: 0.5pt + black)
    } else {
      none
    },
    table.cell(colspan: 1, []),
    table.cell(colspan: 3, align: center, [LES-AV]),
    table.cell(colspan: 3, align: center, [IOSTAR]),
    table.cell(colspan: 3, align: center, [CHASE-DB1]),
    table.header([模型], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU]),
    [FACT], [0.696], [0.670], [0.534], [0.513], [0.477], [0.345], [0.602], [0.573], [0.431],
    [FedDG], [0.736], [0.716], [0.582], [0.688], [0.665], [0.524], [0.650], [0.633], [0.482],
    [GIN-IPA], [0.643], [0.617], [0.473], [0.641], [0.611], [0.472], [0.583], [0.551], [0.412],
    [CSCA], [0.646], [0.624], [0.480], [0.494], [0.485], [0.332], [0.154], [0.233], [0.412],
    [FreeSDG], [0.795], [0.778], [0.659], [0.736], [0.716], [0.582], [0.696], [0.671], [0.533],
    [FD-SDG], [0.754], [0.735], [0.605], [0.743], [0.722], [0.591], [0.707], [0.683], [0.547],
    [BIRF-SDG], [0.787], [0.770], [0.649], [0.748], [0.730], [0.597], [0.729], [0.709], [0.574],
  ),
  caption: [不同血管分割方法的性能比较],
) <tbl-comparison>

从结果可以看出，直接替换完整振幅谱的 FACT 在 IOSTAR 上表现较弱，DICE 仅为 0.513，说明过强的频域替换可能破坏分割所需的信息。FedDG 通过频率信息共享取得了比 FACT 更好的结果，但整体仍低于本文方法。GIN-IPA 的因果增强策略在部分数据集上存在波动，可能与增强强度较大有关。CSCA 虽然引入注意力机制，但在 CHASE-DB1 上 DICE 仅为 0.154，跨域稳定性不足。FreeSDG 在 LES-AV 上获得较高 DICE，但在其他目标域上不如 BIRF-SDG 稳定。FD-SDG 的频率丢弃策略具有一定效果，但缺少频带重要性约束，因此整体性能仍低于本文完整模型。

BIRF-SDG 在三个目标域上的综合指标均达到最优或接近最优。对于 IOSTAR 和 CHASE-DB1 这类与源域差异更明显的数据集，本文方法的提升更为突出，说明频带解耦与重要性感知随机滤波有助于增强模型在未知域中的适应能力。

=== 定性结果

@fig-visual-comparison 展示了不同方法的分割可视化结果。相较于部分基线方法，BIRF-SDG 在细小血管和血管连续性方面表现更稳定，断裂和漏检现象相对更少。对于受到光照变化、病灶干扰或图像质量下降影响的样本，本文方法仍能较好地保留血管边界。这表明频带重要性感知机制能够在增强数据变化的同时减少对关键结构的破坏。

#figure(
  image("../images/visual.png", width: 100%),
  caption: [三种未见目标数据集（IOSTAR、LES-AV、CHASE-DB1）上的血管分割结果可视化比较。每一行展示一个样本；各列依次为原始图像、CSCA、FACT、FD-SDG、FedDG、FreeSDG、GIN-IPA、BIRF-SDG（本文方法）和真实标签（GT）的分割结果。可以看到，BIRF-SDG在多个样本中能够更好地保持血管连续性并减少断裂。],
) <fig-visual-comparison>

=== 训练过程分析

@fig-training-procedure 展示了 BIRF-SDG 的训练流程。预热阶段先让模型学习基础分割能力，增强阶段再引入频带评分和随机滤波，从而避免训练初期过强扰动影响收敛。@fig-random-filter 展示了频带选择和增强图像重建过程，说明评分结果如何指导随机滤波。

== 消融实验

=== 实验设计

为评估各模块的独立作用，本文设置了多组消融变体。基础模型仅包含常规数据增强和分割网络，不使用频域增强；“加频带加权”在基础模型中加入频带评分，但不执行随机滤波；“加随机滤波”进一步采用基于权重的频带滤波；BIRF-SDG 则为同时包含频带评分、随机滤波和高频结构建模的完整模型。

=== 消融结果

@tbl-ablation 给出了各消融变体的实验结果。

#figure(
  table(
    columns: (auto, auto, auto, auto, auto, auto, auto, auto, auto, auto),
    inset: 4pt,
    align: center,
    stroke: (x, y) => if y == 0 or y == 1 or y == 5 {
      (top: 0.5pt + black, bottom: 0.5pt + black)
    } else {
      none
    },
    table.cell(colspan: 1, []),
    table.cell(colspan: 3, align: center, [LES-AV]),
    table.cell(colspan: 3, align: center, [IOSTAR]),
    table.cell(colspan: 3, align: center, [CHASE-DB1]),
    table.header([模型], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU]),
    [基础模型], [0.735], [0.713], [0.581], [0.669], [0.640], [0.502], [0.663], [0.639], [0.496],
    [加频带加权], [0.740], [0.718], [0.587], [0.675], [0.647], [0.509], [0.671], [0.648], [0.505],
    [加随机滤波], [0.745], [0.723], [0.594], [0.692], [0.666], [0.529], [0.678], [0.648], [0.505],
    [BIRF-SDG], [0.787], [0.770], [0.649], [0.747], [0.731], [0.597], [0.729], [0.709], [0.574],
  ),
  caption: [BIRF-SDG 消融实验结果],
) <tbl-ablation>

消融结果表明，频带加权机制能够带来稳定但有限的提升，例如 LES-AV 的 DICE 从 0.735 提升到 0.740，说明频带重要性评分有助于识别关键频率区域。在此基础上加入随机滤波后，LES-AV 的 DICE 进一步提升到 0.745，表明选择性频域扰动能够增加训练样本的有效变化。完整模型在三组数据上取得最优结果，尤其 LES-AV 的 DICE 提升到 0.787，说明高频结构建模对保持血管边界和细小结构具有重要作用。整体来看，三个模块单独使用时提升有限，组合后能够形成更明显的协同效果。

@fig-multistream-network 展示了高频成分机制在网络中的作用位置。高频解码器提供额外的边界结构约束，使分割分支更容易关注血管轮廓。类似地，Grad-CAM@selvaraju2017grad 等可视化方法可用于分析模型关注区域，ZFNet@zeiler2014visualizing 则从特征可视化角度揭示卷积网络的层次化表示。

== 讨论

=== 方法优势

BIRF-SDG 的优势可以概括为四点。第一，增强过程受频带评分约束，避免对全部频率成分进行同等强度的扰动。第二，高频结构约束有助于保留血管边界，降低细小分支在增强过程中被削弱的可能性。第三，方法不需要访问目标域数据，更适合目标域样本难以提前收集的医疗部署场景。第四，频域操作可通过快速傅里叶变换实现，额外计算开销有限，也便于与 MobileNet@howard2017mobilenets、EfficientNet@tan2019efficientnet 等轻量级骨干网络结合。

=== 局限性

尽管实验结果验证了 BIRF-SDG 的有效性，该方法仍存在一些限制。当前频谱被固定划分为 10 个频带，可能无法适配所有数据集的频率分布。评分权重 alpha 仍需人工设置，尚未实现随训练状态自适应调整。此外，本文主要围绕视网膜血管分割设计网络和损失函数，若迁移到其他医学图像分割任务，可能需要重新调整结构。最后，模型尚未显式估计预测不确定性，未来可结合贝叶斯深度学习方法@kendall2017uncertainties 提升可靠性。

=== 未来改进方向

针对上述不足，后续研究可从以下方面改进。首先，可以设计数据驱动的频带划分策略，使频带边界根据图像频谱特性自动调整。其次，可以引入动态超参数更新机制，根据训练反馈自适应调节 alpha 和 beta。再次，可将该框架迁移到肿瘤分割、器官分割等医学图像任务中，以进一步检验其通用性。最后，频域增强还可与混合增强、对抗增强或自监督学习结合，以提升模型在未知域上的稳定性。
