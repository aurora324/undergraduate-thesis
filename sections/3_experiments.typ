= 实验

== 实验设置

=== 数据集

在本部分中，我们在不同域上进行眼底血管分割实验。通过与对比实验的比较，验证了我们实验的性能提升，并说明了我们模型在泛化方面的优越性能。我们使用以下数据集进行实验。

@tbl-dataset 给出了实验数据集信息。本文方法的标注源域为 DRIVE。DRIVE 包含 40 张标注图像（20 张训练，20 张测试），分辨率为 584 乘以 565 像素，作为实验中的主要源域。EyePACS 包含 35106 张未标注图像，仅作为未标注补充数据和多源域基线方法的参考数据，不参与 BIRF-SDG 的监督训练。这些数据集已被广泛用于糖尿病视网膜病变检测研究@ting2017development。

目标域数据集包括 IOSTAR、LES-AV 和 CHASE-DB1。IOSTAR 包含 30 张图像，使用扫描激光眼底镜（SLO）获取，与眼底相机图像具有显著不同的成像特性。LES-AV 包含 22 张高分辨率眼底图像，血管结构复杂。CHASE-DB1 包含 28 张儿童眼底图像，与成人眼底图像存在差异。这些数据集涵盖了不同的成像设备、患者群体和图像质量，为评估模型的泛化能力提供了全面的测试场景。


#figure(
  table(
    columns: (auto, auto, auto, auto, auto, auto),
    inset: 8pt,
    align: center,
    stroke: (x, y) => if y == 0 or y == 1 or y == 6 {
      (top: 0.5pt + black, bottom: 0.5pt + black)
    } else {
      (bottom: 0.5pt + black)
    },
    table.header(
      [数据集], [图像数量], [分辨率], [成像设备], [特点], [用途],
    ),
    [DRIVE], [40 (20训练/20测试)], [584×565], [眼底相机], [标准基准数据集，血管标注精细], [源域],
    [EyePACS], [35106], [多种], [多种眼底相机], [大规模未标注数据], [未标注补充],
    [IOSTAR], [30], [1024×1024], [SLO扫描激光眼底镜], [与眼底相机图像特性差异大], [目标域],
    [LES-AV], [22], [1620×1080], [高分辨率眼底相机], [血管结构复杂，高分辨率], [目标域],
    [CHASE-DB1], [28], [999×960], [儿童眼底相机], [儿童眼底图像，与成人差异大], [目标域],
  ),
  caption: [实验数据集信息对比],
) <tbl-dataset>

=== 实现细节

输入图像被裁剪为 512 乘以 512 像素，所有方法使用相同的数据划分、预处理流程和目标域测试设置。在训练过程中，我们使用 Adam 优化器@kingma2015adam，动量参数 beta1 为 0.9，beta2 为 0.999，初始学习率为 1 乘以 10 的负 4 次方，使用余弦退火策略逐渐衰减至零。训练周期共 50 个 epoch，前 20 个 epoch 预热，后 30 个 epoch 引入增强，批次大小为 4。在医学图像分析中，样本数量与模型性能的关系值得深入研究@liu2022do。AdamW优化器@loshchilov2019decoupled 通过解耦权重衰减进一步提升了训练稳定性。频带数量 n 为 10，频带评分权重 alpha 为 1.0，边界损失权重 beta 为 1.0。为便于频带处理，频谱被划分为 10 个评分频带，结构评分和分割评分按 1 比 1 的权重比例共同决定频带重要性。

#figure(
  grid(
    columns: 1fr,
    row-gutter: 6pt,
    [
      #image("../images/Drive.png", width: 92%)
      #align(center)[(a) DRIVE标注样本]
    ],
    [
      #image("../images/eyepacs.png", width: 92%)
      #align(center)[(b) EyePACS未标注样本]
    ],
  ),
  caption: [源域与未标注补充数据示例。(a) DRIVE数据集样本，包含彩色眼底图像及两位专家标注，分辨率为584×565像素，使用Canon CR5非散瞳相机拍摄。(b) EyePACS数据集样本，包含来自不同来源的眼底图像，分辨率和成像设备多样，通常用作多源域泛化的补充数据。两个数据集在图像质量、亮度和对比度上存在显著差异，体现了真实场景中的域偏移问题。],
) <fig-source-datasets>

=== 评估指标

为评估模型性能，我们采用三种广泛使用的分割指标。DICE 系数（Dice Similarity Coefficient）衡量预测分割与真实标签的重叠程度；马修斯相关系数（Matthews Correlation Coefficient, MCC）综合考虑真阳性、真阴性、假阳性和假阴性；交并比（Intersection over Union, IoU）衡量预测区域与真实区域的交叠比例。设 $"TP"$、$"TN"$、$"FP"$ 和 $"FN"$ 分别表示真阳性、真阴性、假阳性和假阴性，则三种指标定义如下：

$ "DICE" = (2 dot "TP") / (2 dot "TP" + "FP" + "FN") $

$ "MCC" = ("TP" dot "TN" - "FP" dot "FN") / sqrt(("TP" + "FP")("TP" + "FN")("TN" + "FP")("TN" + "FN")) $

$ "IoU" = "TP" / ("TP" + "FP" + "FN") $

这些指标取值范围均为 0 到 1，值越高表示分割性能越好。

== 对比实验

=== 对比方法

我们的对比研究包括以下基线方法。基于傅里叶的方法包括 FACT 和 FedDG，其中 FACT 通过替换完整振幅谱来改进源域图像，FedDG 利用对比学习范式促进多源域间的频率信息共享。多模态分割方法包括 CSCA，在多模态分割框架内整合注意力机制。单源域泛化（SDG）方法包括 GIN-IPA、FreeSDG 和 FD-SDG，其中 GIN-IPA 利用因果启发的数据增强，FreeSDG 结合混合频域处理和边界感知学习，FD-SDG 通过对不同频率带的特征应用基于丢弃的增强来改进泛化。定量分析和视觉比较共同证明了我们方法在实现稳健跨域眼底血管分割性能方面的优越性。

=== 定量结果

@tbl-comparison 总结了我们方法与几种基线模型的性能比较，使用三个常用指标在三个不同的眼底图像数据集上进行评估。

#figure(
  table(
    columns: (auto, auto, auto, auto, auto, auto, auto, auto, auto, auto),
    inset: 4pt,
    align: center,
    stroke: (x, y) => if y == 0 or y == 1 or y == 8 {
      (top: 0.5pt + black, bottom: 0.5pt + black)
    } else {
      none
    },
    table.cell(colspan: 1, []),
    table.cell(colspan: 3, align: center, [LES-AV]),
    table.cell(colspan: 3, align: center, [IOSTAR]),
    table.cell(colspan: 3, align: center, [CHASE-DB1]),
    table.header([模型], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU]),
    [FACT], [0.696], [0.670], [0.534], [0.513], [0.477], [0.345], [0.602], [0.573], [0.431],
    [FedDG], [0.736], [0.716], [0.582], [0.688], [0.665], [0.524], [0.650], [0.633], [0.482],
    [GIN-IPA], [0.643], [0.617], [0.473], [0.641], [0.611], [0.472], [0.583], [0.551], [0.412],
    [CSCA], [0.646], [0.624], [0.480], [0.494], [0.485], [0.332], [0.154], [0.233], [0.412],
    [FreeSDG], [0.795], [0.778], [0.659], [0.736], [0.716], [0.582], [0.696], [0.671], [0.533],
    [FD-SDG], [0.754], [0.735], [0.605], [0.743], [0.722], [0.591], [0.707], [0.683], [0.547],
    [BIRF-SDG], [0.787], [0.770], [0.649], [0.748], [0.730], [0.597], [0.729], [0.709], [0.574],
  ),
  caption: [不同血管分割方法的性能比较],
) <tbl-comparison>

从结果可以观察到，FACT 通过替换完整振幅谱来改进源域图像，但这通常由于关键信息丢失而导致性能下降，在 IOSTAR 数据集上表现最差，DICE 仅为 0.513。FedDG 利用对比学习范式，促进多个源域之间的频率信息共享，性能优于 FACT，但仍不及我们的方法。GIN-IPA 利用因果启发的数据增强，但训练过程中观察到的不稳定性表明其增强策略可能过于激进，导致在某些数据集上性能波动较大。CSCA 在多模态分割框架内整合注意力机制，但在跨域泛化方面表现不佳，特别是在 CHASE-DB1 上 DICE 仅为 0.154。FreeSDG 结合混合频域处理和边界感知学习，在 LES-AV 上取得了较好的结果，DICE 为 0.795，但在其他数据集上略逊于我们的方法。FD-SDG 通过对不同频率带的特征应用基于丢弃的增强来改进泛化，性能稳定但不及 BIRF-SDG。

BIRF-SDG 在所有三个数据集上都取得了最佳或接近最佳的性能，证明了其优越的泛化能力。在对比实验中，我们的 BIRF-SDG 通过解耦不同频率带和自适应随机滤波来增强模型泛化能力，其性能优于不依赖额外数据的最佳方法，特别是在 IOSTAR 和 CHASE-DB1 这两个更具挑战性的数据集上，我们的方法显示出明显的优势。

=== 定性结果

@fig-visual-comparison 展示了不同方法之间的比较，从视觉对比可以看出我们提出方法的有效性。BIRF-SDG 能够更好地保持血管的连续性，减少断裂现象。我们的方法能够检测到更多的细小血管，这在其他方法中往往被忽略。某些图像中由光照和疾病引起的噪声会影响分割性能，而 BIRF-SDG 能够有效地抑制这些噪声。与其他竞争方法相比，我们的模型清晰地保留了眼底血管结构的边界。数据集质量的下降对模型的泛化能力提出了挑战，BIRF-SDG 通过采用 BIA 机制在频域视角解耦频率带，并通过频带随机滤波机制在分割过程中消除域差异，从而在这些挑战性场景下仍能保持较好的性能。

#figure(
  image("../images/visual.png", width: 70%),
  caption: [三种未见目标数据集（IOSTAR、LES-AV、CHASE-DB1）上的血管分割结果可视化比较。每一行展示一个样本；各列依次为原始图像、CSCA、FACT、FD-SDG、FedDG、FreeSDG、GIN-IPA、BIRF-SDG（本文方法）和真实标签（GT）的分割结果。可以看到，BIRF-SDG在多个样本中能够更好地保持血管连续性并减少断裂。],
) <fig-visual-comparison>

=== 训练过程分析

@fig-training-procedure 展示了 BIRF-SDG 的训练流程。通过预热阶段与增强阶段的分离，模型先学习稳定的基础分割能力，再引入频带评分与随机滤波增强，从而降低早期训练不稳定对最终性能的影响。@fig-random-filter 展示了频带选择与增强图像重建过程，说明频带重要性评分机制能够为后续随机滤波提供依据。

== 消融实验

=== 实验设计

为验证 BIRF-SDG 各个组件的有效性，我们进行了系统的消融实验。通过对三个模块进行消融，我们展示了基于重要性感知的 SDG 框架各部分的有用性。我们设计了以下变体：基础模型仅使用标准的数据增强（随机翻转、旋转）和分割网络，不使用频域增强；加频带加权在基础模型上添加频带评分机制，但不进行随机滤波；加随机滤波完整模型，包含频带评分和基于权重的随机滤波；BIRF-SDG 完整模型包含所有组件。

=== 消融结果

@tbl-ablation 展示了消融实验的结果。

#figure(
  table(
    columns: (auto, auto, auto, auto, auto, auto, auto, auto, auto, auto),
    inset: 4pt,
    align: center,
    stroke: (x, y) => if y == 0 or y == 1 or y == 5 {
      (top: 0.5pt + black, bottom: 0.5pt + black)
    } else {
      none
    },
    table.cell(colspan: 1, []),
    table.cell(colspan: 3, align: center, [LES-AV]),
    table.cell(colspan: 3, align: center, [IOSTAR]),
    table.cell(colspan: 3, align: center, [CHASE-DB1]),
    table.header([模型], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU], [DICE], [MCC], [IoU]),
    [基础模型], [0.735], [0.713], [0.581], [0.669], [0.640], [0.502], [0.663], [0.639], [0.496],
    [加频带加权], [0.740], [0.718], [0.587], [0.675], [0.647], [0.509], [0.671], [0.648], [0.505],
    [加随机滤波], [0.745], [0.723], [0.594], [0.692], [0.666], [0.529], [0.678], [0.648], [0.505],
    [BIRF-SDG], [0.787], [0.770], [0.649], [0.747], [0.731], [0.597], [0.729], [0.709], [0.574],
  ),
  caption: [BIRF-SDG 消融实验结果],
) <tbl-ablation>

从消融实验结果可以得出以下结论。通过频带加权机制，我们将图片解耦为对结构信息重要的频带和可以有效增强的频带，相比基础模型，添加频带加权后性能有小幅提升，LES-AV 的 DICE 从 0.735 提升到 0.740，说明该机制能够有效识别关键频带。通过频带随机滤波机制，我们对图像进行泛化并提高模型的泛化能力，添加随机滤波后，性能进一步提升，LES-AV 的 DICE 从 0.740 提升到 0.745，证明了随机滤波在增强数据多样性方面的有效性。完整模型（包含高频成分机制）在所有指标上都取得了最佳性能，特别是在 LES-AV 上，DICE 从 0.745 大幅提升到 0.787，说明高频成分机制对于保留血管结构至关重要。这三个模块的协同作用显著提高了模型在单源血管分割任务中的泛化能力，单独使用任一模块只能带来有限的提升，而组合使用则产生显著的协同效应。

@fig-multistream-network 展示了高频成分机制在网络结构中的位置。通过高频解码器提取的显著结构表示，模型能够更好地关注血管边界信息，从而提高分割精度。神经网络可视化技术如Grad-CAM@selvaraju2017grad 可以进一步帮助我们理解模型的决策过程，而ZFNet的可视化方法@zeiler2014visualizing 则揭示了卷积神经网络学习到的层次化特征。

== 讨论

=== 方法优势

BIRF-SDG 的主要优势在于四个方面。首先是自适应增强，与传统方法对所有频率成分进行同等处理不同，我们的方法能够自适应地识别关键频带，实现有选择性的增强。其次是结构保留，通过高频成分机制，我们能够在增强过程中保留血管的结构信息，避免了过度增强导致的性能下降。再次是无需目标域数据，与域适应方法不同，我们的方法在训练过程中不需要任何目标域数据，更符合实际应用场景。最后是计算效率高，频域操作可以通过快速傅里叶变换高效实现，不会显著增加训练时间。我们的方法可以很容易地部署到轻量级网络架构如MobileNet@howard2017mobilenets 和EfficientNet@tan2019efficientnet 上，适用于资源受限的医疗设备。

=== 局限性

尽管 BIRF-SDG 取得了良好的性能，但仍存在以下局限性。频带划分的粒度方面，当前方法将频谱划分为固定的 10 个频带，可能无法捕获更细粒度的频率特征。超参数敏感性方面，评分机制中的超参数 alpha 需要根据具体任务进行调整，缺乏自适应的调节机制。适用范围方面，当前方法主要针对视网膜血管分割任务，对于其他类型的医学图像分割任务，可能需要调整网络架构。此外，当前方法缺乏对预测不确定性的显式建模，贝叶斯深度学习方法@kendall2017uncertainties 可能有助于提高模型的可靠性。

=== 未来改进方向

针对上述局限性，未来的研究可以从以下方向进行改进。自适应频带划分方面，探索基于数据驱动的自适应频带划分方法，而不是使用固定的均匀划分。超参数自适应方面，设计自适应的超参数调节机制，根据训练过程中的反馈动态调整 alpha 和 beta。扩展到其他任务方面，将方法扩展到其他医学图像分割任务，如肿瘤分割、器官分割等。结合其他增强技术方面，将频域增强与其他类型的增强技术（如混合增强、对抗增强）相结合，进一步提高泛化能力。
