= 绪论

== 研究背景与意义

视网膜血管分割是医学图像处理领域的一个重要研究方向，对于眼科疾病的早期诊断和治疗具有重要意义 @gulshan2016development @orlando2018towards。通过分析眼底图像中的血管结构，医生可以检测出糖尿病视网膜病变、青光眼、高血压视网膜病变等多种疾病。然而，由于不同医疗机构使用的成像设备、拍摄参数和患者群体的差异，眼底图像在亮度、对比度、颜色分布等方面存在显著差异，这给自动化血管分割算法带来了巨大挑战。

在视网膜血管分割任务中，分割模型的泛化性能对于其实际应用至关重要。由于标注眼底图像中血管结构的成本高昂以及隐私问题的考量，足够大且多样化的数据集往往难以获取。专业眼科医生需要花费大量时间对每一张眼底图像进行精细标注，标注一张高质量的血管分割图可能需要数小时的时间。此外，由于医疗数据的敏感性，不同机构之间的数据共享面临严格的隐私保护限制。因此，在实际应用中，通常只有源数据集可用于模型训练。

这导致仅在单一源数据集上训练的模型在应用于具有不同特征的目标数据集时常常面临困难。例如，在一个医院的数据集上训练良好的模型，在应用到另一个医院的数据时，分割性能可能会显著下降。这一问题的关键根本原因是域偏移（domain shift），即不同数据集之间特征分布的不匹配 @ganin2015unsupervised。域偏移会导致模型过度拟合源域的特定特征，而无法泛化到具有不同分布的目标域。为克服这一挑战，域泛化（domain generalization）作为一种有效策略受到关注 @cugu2022attention。域泛化旨在利用源域数据训练模型，使其能够泛化到未见过的目标域。具体而言，单源域泛化（single-source domain generalization, SDG）旨在利用单一源数据集进行训练，解决域偏移问题并提升模型在未见目标域上的性能 @ouyang2023causality。其核心目标是从单一源数据中学习更多具有泛化性的特征，从而使模型能够有效适应数据分布不同的目标域。

图像增强是域泛化方法中广泛使用的策略。数据增强通过引入多样化的变换来模拟域间分布变化，使模型能够学习更具泛化性的特征表示。在单源域的约束下，传统的空间域增强技术（如裁剪、旋转、缩放和颜色变换）@hu2022domain 可能不足以充分捕捉目标域的完整分布变化。近年来，频域增强在域泛化中显示出巨大潜力。与空间域增强不同，频域增强通过对图像频谱进行操作，能够生成更多样化的训练样本。基于傅里叶的数据增强（FDA）通过交换目标域和源域的振幅谱来创建具有目标域风格的合成图像 @xu2021fourier，振幅扰动方法则通过随机扰动振幅谱进一步增强数据多样性 @li2023frequency。然而，现有的频域增强方法存在以下问题：缺乏选择性，现有方法通常对所有频率成分进行同等处理，可能导致关键结构信息的丢失；增强强度不可控，过度的数据增强可能导致模型性能不稳定，甚至造成性能下降或模型崩溃；缺乏自适应机制，现有方法无法根据任务需求自适应地选择需要增强的频率带。

#figure(
    grid(
    columns: (1fr, 1fr),
    gutter: 10pt,
  image("../images/domain.png", width: 150%),
  image("../images/diabetic.png", width: 85%),
  ),
  caption: [左：频域变换流程示意图。通过 FFT 将图像从空间域转换到频域，在频域进行操作后通过 IFFT 转换回空间域。右：糖尿病视网膜病变示例图像，展示了眼底图像中常见的病变特征，包括微动脉瘤、出血点和渗出物等。], 
) <fig-domain-process>

@fig-domain-process 展示了频域变换的基本流程，说明了 FFT 和 IFFT 在图像处理中的应用。

== 国内外研究现状

域泛化是机器学习中一个重要的研究方向，旨在解决训练数据和测试数据分布不一致的问题。早期的域泛化方法主要关注多源域场景，即利用多个源域数据来学习域不变特征。然而，在实际应用中，获取多个源域数据往往是不现实的。因此，单源域泛化（SDG）逐渐成为研究热点。现有的单源域泛化方法主要可以分为三类：基于数据增强的方法、基于特征对齐的方法和基于正则化的方法。

基于数据增强的方法通过对训练数据进行各种变换来增加数据多样性。传统的空间域增强技术包括裁剪、旋转、缩放和颜色变换等，然而这些方法可能不足以充分捕捉目标域的完整分布变化。近年来，Mixup、CutMix 和 AutoAugment 等高级增强技术被提出，可以生成更多样化的训练样本。

基于特征对齐的方法通过学习域不变的特征表示来减少域间差异，例如通过对抗学习或元学习来提取跨域共享的特征。基于正则化的方法则通过在训练过程中添加正则化项来防止模型过拟合源域数据，从而提高泛化能力，例如 Dropout、Batch Normalization 和 Label Smoothing 等技术。

近年来，频域增强在域泛化中显示出巨大潜力。与空间域增强不同，频域增强通过对图像频谱进行操作，能够生成更多样化的训练样本。基于傅里叶的数据增强（FDA）通过交换目标域和源域的振幅谱来创建具有目标域风格的合成图像 @xu2021fourier，振幅扰动方法则通过随机扰动振幅谱进一步增强数据多样性 @li2023frequency。然而，现有的频域增强方法存在以下问题：缺乏选择性，现有方法通常对所有频率成分进行同等处理，可能导致关键结构信息的丢失；增强强度不可控，过度的数据增强可能导致模型性能不稳定，甚至造成性能下降或模型崩溃；缺乏自适应机制，现有方法无法根据任务需求自适应地选择需要增强的频率带。

== 研究内容与贡献

针对上述问题，本研究提出了一种基于频带重要性感知的随机频率滤波方法（BIRF-SDG）。该方法通过自适应地评估不同频率带的重要性，实现了有选择性的频域增强，既增加了数据多样性，又保留了关键的结构信息。

本研究的主要贡献可总结如下。首先，我们提出了一种新的单源域泛化框架，这是一种基于频域增强技术的视网膜血管分割单源域泛化方法，通过频带重要性感知机制来缓解由数据集间特征分布差异引起的域偏移。其次，我们设计了频带重要性评分机制，该机制通过检查不同频率带对分割性能和结构信息的影响来对图像的频域进行分级，能够自适应地识别对结构保留和分割任务至关重要的频率带。再次，我们提出了加权随机频带滤波策略，这是一种基于频带权重的随机频带滤波机制，仅对不影响结构信息的频率带进行增强，在增加数据多样性的同时，避免了过度增强导致的性能下降。最后，我们进行了全面的实验验证，在多个视网膜血管分割数据集上进行了跨域评估，包括对比实验和消融研究，证明了我们方法的优越泛化能力。

== 论文组织结构

本文的组织结构如下。第一章（绪论）介绍了研究背景、国内外研究现状、研究内容与贡献。第二章（相关工作）回顾了视网膜血管分割、域泛化和频域分析的相关研究。第三章（研究方法）详细介绍了 BIRF-SDG 框架的各个组成部分，包括频域增强、频带重要性感知随机滤波和分割网络整体结构。第四章（实验）描述了实验设置、对比实验结果和消融研究分析。第五章（结论）总结了本文的工作，讨论了研究意义，并展望了未来研究方向。
