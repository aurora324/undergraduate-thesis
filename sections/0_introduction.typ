= 绪论

== 研究背景与意义

眼底血管的走向、宽度和分支形态与多类眼科疾病及全身性疾病相关，因此自动血管分割是眼底图像分析中的基础环节@gulshan2016development@orlando2018towards。可靠的血管掩膜能够为糖尿病视网膜病变、青光眼和高血压相关眼底改变等疾病的观察提供结构依据。然而，眼底图像常受医院来源、拍摄设备和采集流程影响，在亮度、对比度、颜色分布和噪声水平上呈现差异，导致分割模型在临床真实场景中容易出现性能波动。

该任务的另一个难点来自医学数据本身。血管标注通常需要专业人员逐像素完成，耗时长、成本高，并且不同机构之间的数据共享还受到隐私保护和伦理审批的限制。因此，研究者在训练模型时往往只能获得规模有限、来源单一的标注数据。如果模型过度依赖源数据集中的成像风格或局部纹理，它在其他医院或其他采集设备上的表现就可能明显下降。

训练数据和测试数据分布不一致时，模型面临的问题通常称为域偏移（domain shift）@ganin2015unsupervised。域泛化（domain generalization）希望在不使用目标域样本的前提下，从源域数据中学习可迁移的表示@cugu2022attention。进一步地，单源域泛化（single-source domain generalization, SDG）只允许训练数据来自一个源域@ouyang2023causality，这一设定更贴近医学图像标注数据有限的情况，但也放大了源域偏差带来的风险。因此，本文关注如何仅依赖单一标注眼底数据集，训练出能够适应未见目标域的血管分割模型。

提高泛化性能时，数据增强是最常用的训练策略之一。现有空间域增强通常采用裁剪、旋转、缩放和颜色扰动等操作@hu2022domain，但这些变化不一定能覆盖不同眼底数据集之间的成像风格差别。频域增强则直接处理图像频谱，为模拟风格和结构变化提供了另一种途径。已有研究通过振幅谱交换或频谱扰动扩展训练样本@xu2021fourier@li2023frequency，但这类方法往往没有区分不同频率成分对分割任务的重要程度：关键频带被过度扰动时，血管边界和细小分支可能受损；扰动过弱时，又难以形成有效的跨域变化。因此，本文尝试设计一种频带重要性感知的选择性增强方法。

== 国内外研究现状

域泛化研究的核心目标，是让模型在训练分布之外仍能保持稳定预测能力。早期工作通常默认训练阶段可以获得多个源域，并利用跨域特征对齐、对抗学习或元学习提取更稳定的表示。然而在医学图像场景中，多机构数据获取困难，数据格式和标注标准也可能不一致，因此只依赖一个源域的 SDG 逐渐成为更符合实际部署条件的研究方向。现有 SDG 方法大致可分为数据增强、特征约束和正则化三类。

数据增强方法通过扩展训练样本的变化范围，削弱模型对源域外观模式的依赖。除常规几何变换和颜色变换外，Mixup、CutMix 和 AutoAugment 等方法也可构造更复杂的训练样本。特征约束方法侧重于表示学习，通常借助对抗训练、元学习或特征解耦提升特征跨域稳定性。正则化方法则利用 Dropout、Batch Normalization 和 Label Smoothing 等机制减轻过拟合，使模型在未知数据上更加稳健。

近年来，频域建模为 SDG 提供了新的切入点。傅里叶变换能够把图像表示为不同频率成分的组合，其中振幅谱更容易反映成像风格，相位谱则与结构布局联系更紧密。FDA 通过替换振幅谱生成目标域风格样本@xu2021fourier，振幅扰动方法通过随机改变频谱扩大训练分布@li2023frequency。这些方法能够增加样本多样性，但通常没有显式判断哪些频带承载血管结构，也难以控制增强对分割结果的影响。基于这一问题，本文从频带重要性出发，在结构保持和跨域变化之间寻求平衡。

== 研究内容与贡献

针对上述问题，本文提出一种基于频带重要性感知的随机频率滤波方法 BIRF-SDG。该方法通过评估不同频带对结构保留和分割性能的影响，选择性地对非关键频带进行随机滤波，从而在增强数据多样性的同时降低关键血管结构被破坏的风险。

本文的主要贡献包括四点。第一，提出面向视网膜血管分割的单源域泛化框架，从频域角度缓解数据集间成像差异带来的域偏移。第二，设计频带重要性评分机制，分别衡量频带对结构信息和分割结果的影响，从而识别需要保护的关键频带。第三，提出加权随机频带滤波策略，使增强过程集中作用于对结构影响较小的频率区域，降低过度增强导致性能下降的可能性。第四，在多个跨域眼底血管分割数据集上进行对比实验和消融分析，验证所提方法在未见目标域上的泛化效果。

== 论文组织结构

后续章节安排如下。第二章回顾视网膜血管分割、域泛化和频域分析方面的相关研究。第三章介绍 BIRF-SDG 的整体设计，并说明频域增强、频带重要性感知随机滤波和多流分割网络等关键模块。第四章给出实验设置、对比实验和消融实验结果。第五章总结本文工作，并讨论可能的改进方向。
